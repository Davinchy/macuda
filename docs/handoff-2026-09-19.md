# Handoff — 2026-09-19, before Antonio's host reboot

**Read this first when resuming.** Everything below is in this folder (`/Volumes/512SSD/EGPU_MAC_Nvidia`), scoped
here on purpose — do not read from or write to `/Volumes/512SSD/EGPU` (a separate, shared tree belonging to
Antonio's other sessions; see the standing rule in memory, `egpu-shared-tree-boundary`).

## State at handoff time

- `main` tip: `b76c16a`, tree clean, everything committed. Not yet pushed to `origin/main` — check before assuming
  it's there.
- Card: preflight clean (`VERDICT: OK to proceed`), lock free, no step in flight. Safe to reboot.
- Antonio was physically present for this session's hardware work (he replugged the card at the start of it).

## What this session did: finished the scoped next step from the 09-18 handoff, found the next one

The 09-18 handoff's "scoped next step, not yet started" was per-launch descriptor diffing (diff each launch's own
sub-block individually within a flush, instead of one envelope over the whole flush). This session implemented,
crashed it once on real hardware, fixed it, then found and partly fixed a second problem, and ends with a third
one clearly scoped but not started. In order:

1. **Implemented per-launch diffing** (`c728085`) — `chain[]` gained a `.len` field (QMD slot + that launch's own
   cbuf0 size, which varies per kernel), and `tinynv_exec_flush` diffs each launch's own span individually against
   `last_delivered` instead of one min-max envelope over the whole flush. All-or-nothing per flush: every launch's
   diff has to fit before anything is committed, or the flush falls through to the existing full-copy path
   unchanged — no risk of double-delivering bytes.

2. **Crashed on hardware, root-caused, fixed** (`51a1011`) — first real hardware run (`TINYNV_DELTA_DELIVERY=1
   TINYNV_INLINE_PEND=128`, Qwen3.8-27B greedy decode) aborted: `tinycudart` error, "an inline upload is a whole
   number of dwords, not 1542 bytes". The byte-precise diff scan produced spans with no alignment guarantee, and
   `tinynv_cmd_inline_upload` requires both the length and destination to be 4-byte aligned (`submit.c:65-67`).
   Fixed by widening each launch's diff bounds to the nearest dword on both sides — safe because a launch's own
   span is itself 256-byte aligned with a 256-byte-rounded length, so widening can never cross into a neighbour's
   span. Worth noting: the single-envelope version this replaced (`a4f9010`) had the identical latent bug — it
   just never reached the inline-upload call often enough to hit it, because the whole-flush envelope almost
   always read as too big to fit and fell back first. Re-validated: op-verify 450/450 at three depths,
   byte-identical decode, no crash, including at the setting that crashed before.

3. **Found and partly fixed a real bottleneck** (`b76c16a`) — even fixed, the mechanism was landing on only 5 of
   1223 flushes in a 96-token decode. Root cause: delta patches were queued through the same `inline_pend` list
   `TINYNV_INLINE_UPLOAD` uses for its own per-launch copies (default budget: 16 entries / 8 KB), and that shared
   list was mostly spent before delta delivery's own check ever ran. Fixed by writing delta patches directly into
   the chain's own delivery batch after `batch_begin()` (reserving their own room in `want_dwords`) instead of
   queuing them — the two mechanisms no longer compete for the same held list. Also added a separate aggregate cap,
   `TINYNV_DELTA_AGGREGATE_MAX` (64 KB, explicitly marked provisional in its own comment — not swept), since the
   old `TINYNV_INLINE_MAX` (4096 bytes) prices one small copy against the copy engine, not a whole flush of patches
   against the ~800 µs full-envelope fallback they actually compete with.

   **Still only 5 of 1223 flushes fire, and this session found out why — that's the real finding, not the two
   fixes above.** A one-shot diagnostic (gated behind `TINYNV_DELTA_VERBOSE`, first three flushes only, still in
   the tree) showed: nearly every one of a chain's 128 launches carries *some* genuine per-token change, not just
   the one or two that looked special before the budget was untangled. Span widths are consistently ~1400-1800
   bytes, but the bytes that actually differ inside each span are only ~40-240 of them — 3-15%, usually ~10-13%.
   **The per-launch min-max envelope is overestimating by roughly 7-10x** — the exact same shape of bug that
   motivated per-launch diffing over the original whole-flush envelope, recurring one level down. Extrapolated
   across a full 128-launch chain, genuine content is probably ~20-25 KB against the ~185 KB the envelope currently
   reports — which is why raising the aggregate cap (tried 16 KB, then 64 KB) mostly just moved the miss point
   rather than fixing anything: the envelope is the binding constraint now, not the budget.

   Correctness held through all of it: op-verify 450/450 at chain depths 32/64/128, 96-token greedy decode
   byte-identical to the reference text, across every cap value tried, no crash.

## The scoped next step: sub-launch diffing, not yet started

Diff **within** each launch's own span too, not just per-launch. Same pattern as this file's whole history so far
— whole-flush envelope, then per-launch envelope, then untangling the delivery budget, now sub-launch envelope is
where the same overestimate keeps showing up.

**What it means concretely.** Right now, for each launch, `tinynv_exec_flush`'s check pass finds one `(dlo, dhi)` —
the first and last differing byte in that launch's `[lo, hi)` span — and treats everything between them as dirty.
Instead, find the actual differing *runs* within that span (a simple pass: track a run while consecutive bytes
differ, close it when they stop, merge two nearby runs if the gap between them is small enough that a second
`tinynv_cmd_inline_upload` call costs more than the merge would — each call is a fixed 8-dword overhead
(`TINYNV_INLINE_DWORDS`), so gaps under roughly that many bytes are cheaper to include than to split around).

**What's reusable, what's new:**
- Reusable as-is: the dword-alignment widening (`dlo &= ~3; dhi = (dhi+3) & ~3`), the all-or-nothing per-flush
  commit discipline, the direct-into-`batch_begin`'s-batch delivery mechanism from `b76c16a`, the aggregate cap
  concept (though its value will need re-tuning once sub-spans are real — it was sized against the *current*
  overestimating envelope).
- New: a bounded per-launch sub-span list (the current `plo[]`/`phi[]` are one `uint64_t` per launch;
  sub-launch diffing needs several per launch — a flat `{lo, hi}` list across the whole flush, capped at some
  total count, is probably simpler than a 2D per-launch array). A cap on how many sub-spans one launch may
  produce before that launch (and so the whole flush, same all-or-nothing rule) is treated as "did not fit" and
  falls back — this is the new failure mode to reason about carefully, on top of the existing per-span and
  aggregate-byte caps.
- The gap-merge threshold is a new tunable with no measured default yet — start from the 8-dword-per-call
  arithmetic above as a first estimate, expect to actually sweep it against hardware data the way `TINYNV_CHAIN_DEPTH`
  and `TINYNV_INLINE_PEND` were, not just reason it out.

**Validation, same discipline as every lever in this file:** op-verify 450/450 at chain depths 32/64/128,
byte-identical greedy decode against the reference text, `TINYNV_DELTA_VERBOSE` re-checked to confirm the new
sub-span accounting actually engages and the genuine-vs-envelope gap actually closes (re-run the same diagnostic
this session added — it's still in the tree, gated behind the env var, first three flushes only). Needs the card;
needs Antonio's go; does not need his physical presence — this is the same risk tier as everything landed this
session, not the launch-chain-replay idea (still separate, still untouched, still gated on Antonio being
physically present regardless of card availability — see `disagg-inference-experiment` / the CUDA-graphs
investigation in memory for that one, unrelated to this).

## Process notes worth carrying forward

- Full detail of the per-launch-diffing investigation (the crash, the budget untangling, the envelope-overestimate
  diagnostic) is in this session's own record; nothing further to load from memory beyond what's summarized above
  — the design doc (`docs/driver/libtinynv-design.md`) has not yet been updated with this session's findings and
  should probably get a `SS4g` or similar before this goes much further, the way `SS4d`-`SS4f` record the launch-path
  history.
- Earlier in this session, another of Antonio's sessions (`egpu-26`) reached out repeatedly for card scheduling and
  patch review. Card-scheduling coordination was briefly engaged (with Antonio's explicit authorization) before
  Antonio corrected that this session should work independently — `egpu-26`'s project is not this one. No shared-
  tree writes happened at any point (declined a request to post to their `SHARED-STATUS.md`); nothing from that
  thread affected anything in this file.
- `tools/wtgate.sh` (the standard gate script) does not run as committed in this checkout — it assumes a worktree
  layout where `cuda-shim` is the repository root, but here it is a subdirectory of a larger monorepo. Validation
  this session was done directly in the live tree instead (`cuda-shim/` `make`, relink via `build/link-null.sh`
  with **absolute** output paths — a relative one resolves against the wrong directory partway through that
  script and fails opaquely). `wtgate.sh` itself was not fixed; if it's wanted working, that's a separate small
  task.

---

# Resumed after the reboot, 2026-09-19 (later the same morning)

**Read this part first if resuming again.** Same scope rule as above: everything here is in this folder; the EGPU
tree is read for its lock and nothing else.

## State now

- `main` tip: `86805a2` plus this doc commit. Tree clean. Pushed to `origin/main` right after this commit - `git
  status -sb` says whether that landed (the four commits above it were still unpushed at the reboot).
- **Card: preflight clean, but the shared lock is held by another session.** At 08:2x: Thunderbolt/PCI link up,
  DART absent, dext pid 602 started 08:14:58 and unchanged since the record (a normal post-reboot instance, nothing to
  clear). `tools/gpu-lock.sh status` shows `session=A since=2026-09-19T08:27:10 what=recovery boot after 133: cd
  /Volumes/512SSD/EGPU/cuda-shim-e && ... vm/vmctl.sh start` - the VM track in the EGPU tree, holding the lock by
  hand (no pid), so it stays held until that session releases it. **Not this project's lock to touch.**
  `tools/nv_shim_step.sh` refuses with `LOCKED` until it is free; that refusal is correct and is the only thing
  standing between the step below and the card.
- Binaries: `cuda-shim/build/bin/{llama-simple,test-backend-ops,llama-bench}-null` relinked at `86805a2` (build id
  found x1 in each, checked with `strings | grep -cx`). `llama-server-null`, `llama-speculative-simple-null`,
  `sd-cli-null`, `llama-mtmd-cli-null` were NOT relinked and still carry older ids.

## What this session did: built the scoped next step, offline only

Sub-launch diffing, exactly as scoped above, landed as `86805a2`. The commit message has the full account; the
short version: `tinynv_delta_runs` finds the runs of *dwords* that differ within each launch's span (no more byte
bounds to widen), merges runs across gaps of up to `TINYNV_DELTA_GAP` bytes (default 32 = the 8 header dwords a
second inline-upload call costs, tie merges), keeps a flush's runs in one flat list with one slot reserved per
launch still to come (so the list can never cost a flush its fast path - a launch that overflows or whose runs cost
more than its envelope goes out as the envelope, `b76c16a`'s behaviour), prices the aggregate cap in pushbuffer
dwords *with headers* through one helper the reservation and the emit loop both use, and makes that cap a knob
(`TINYNV_DELTA_AGGREGATE_KB`, default 128, at most 256). A run over 4 KB is split at emit. The only fallback left
is the aggregate cap. Design history is now `docs/driver/libtinynv-design.md` §4h.

Offline evidence: `test_delta` (17,100 checks; two planted defects seen to fail first, 356 and 3 failures
respectively, then restored), full `make -C cuda-shim test` green. Along the way: the libtinynv Makefile's
stale-object check was inspecting a leftover `libtinynv/build/` from an old standalone build rather than the
`$(BUILD)` the top-level passes, so it now takes `$(BUILD)/src`. `tools/wtgate.sh` is still not runnable from this
layout (unchanged, same note as above).

## The card step, ready to run: needs Antonio's go AND a free lock

Does not need his physical presence - same risk tier as every step this morning: a driver change on the launch path
that is off by default (`TINYNV_DELTA_DELIVERY` unset = every path unchanged), and on, falls through to the
unchanged full-delivery paths whenever it does not fit. The failure this feature can produce is a stale descriptor
dword the kernel reads as a parameter - wrong text, or a kernel faulting on garbage - which nothing on the card
reports, so op-verify at three depths goes first and the byte-compare is the gate, in that order.

1. **Op-verify at three depths**, expected `450/450` each:
   `TINYNV_DELTA_DELIVERY=1 TINYNV_CHAIN_DEPTH=<32|64|128> sh tools/nv_shim_step.sh A opverify`
   Refusal readings: any `FAIL`; `CRASHED`; `VOID`; a `tinycudart` error naming the command buffer ("holds N dwords
   and the batch needs more" - that would be the reservation and the emit loop disagreeing, which `delta_span_dwords`
   exists to make impossible); or "an inline upload is a whole number of dwords" (should be impossible now - runs are
   dwords by construction).
2. **The 96-token greedy text, byte-compared**:
   `TINYNV_DELTA_DELIVERY=1 TINYNV_DELTA_VERBOSE=1 sh tools/nv_shim_step.sh A simple models/Qwen3.8-27B-UD-Q4_K_M.gguf 96 'The three laws of thermodynamics, explained for a bright twelve-year-old, are:'`
   then `cmp` the new `logs/shim-simple-*.txt` against `logs/shim-simple-20260914-225831.txt` (md5
   `3f3f82f729e01131d53d162fceb235aa`; this morning's runs at 07:58 and 08:01-08:04 all matched it).
   Expected in the log: the startup line naming both knobs as "the default"; three `libtinynv: delta flush:` lines
   whose `genuine` is a few tens of KB against an `envelope` of ~185 KB (the 7-10x gap closing); a `delta runs:`
   line per changed launch showing a handful of runs each; and the exit summary `delta delivery patched N flushes`
   with N in the hundreds or above out of ~1223, not 5. Refusal readings: **text differs - stop, that is the
   correctness failure**; a crash; N still near 5 with `delta miss` lines citing `TINYNV_DELTA_AGGREGATE_KB` (that
   is a budget to sweep, not a bug, but say so before sweeping).
3. **MoE, same shape**: an interleaved pair of `simple` runs on `models/Qwen3.5-35B-A3B-Q4_K_M.gguf` (96 tokens,
   flag off then on, same minutes), `cmp` the two texts. There is no committed MoE reference text; the pair is the
   reference.
4. **Only then, speed** - and read the token period, not the flush count: `tools/ab_pair.sh` interleaved tg128 with
   the flag off/on, dense and MoE. The mechanism is per flush; the stall it aims at is per token boundary (§4f), so
   a flush count that goes from 5 to 1200 is necessary and not sufficient. If it moves nothing, the boundary
   instrument in §4f is where to look next, not this code.
5. **Sweeps, if 2 reads right and 4 shows anything**: `TINYNV_DELTA_GAP` 0/16/32/64/128 and
   `TINYNV_DELTA_AGGREGATE_KB` 32/64/128/256, each an interleaved pair, each reading `delta flush:` and the token
   period. Neither default has been measured against a neighbour.

Nothing else is pending on the card. The launch-chain-replay idea remains separate, untouched, and gated on Antonio
being physically present regardless of card availability (see memory, `disagg-inference-experiment`).

---

# The card session, 2026-09-19 08:45-09:1x: one incident, then the lever measured

## Incident: link down at the first open, 08:45:5x - not this driver's doing, and replugged by Antonio

Timeline, all read-only from this side (the other sessions' `SHARED-STATUS.md` in the EGPU tree was read for the
lock history and nothing else):

- 08:34:21-08:34:46: the VM track's run 134 (`cuda-shim-e`, `tinynvd`) took its "reset-on-resident" path - firmware
  resident from an earlier boot, bus mastering cleared, `CMD_RESET` requested - refused at a GPFIFO alloc and exited.
  Its harness then held the shared lock for ten minutes typing into a dead console; Antonio killed it and freed the
  lock by hand at 08:45.
- 08:45:52: this session's first step ran preflight - PCI "Link up", DART absent, dext pid 602 unchanged - and
  acquired the lock at 08:45:54.
- The driver's very first register read (`NV_PMC_BOOT_0`, `dev.c:80`, after the routine warm-card reset the open
  does) returned all ones. Nothing of this session's code had run: the engine table that a healthy open prints next
  never appeared, and `TINYNV_DELTA_DELIVERY` only acts at a flush. op-verify then failed 0/450 over 123 s of
  refused allocations.
- After: preflight "5090 link NOT up", VERDICT ABORT; device still enumerated, dext pid unchanged, DART absent, the
  Thunderbolt tunnel to the box still up. The same signature as the other project's run-133 link-down at 05:3x, which
  they ruled the host-reboot class.
- Antonio: "killing the job killed the card", then "I replugged" (08:50:47, new dext pid 33020). Cleared the
  dext-instance record under the standing authorization (`tools/dart-stale.sh --clear`, second preflight VERDICT OK,
  link up, DART absent) and resumed.

No card action was taken between the ABORT reading and the replug. The two `log show` attempts found nothing
without sudo; if the exact moment matters, `! sudo log show --start "2026-09-19 08:44:00" --end "2026-09-19
08:48:00" --predicate 'process == "kernel"'` from Antonio's own prompt is the read.

## The lever, measured: inline patching loses, token-aligned inline patching wins

Every number below is tg128 from `tools/nv_shim_step.sh A bench` (`-r 3`), interleaved off/on in the same minutes,
binaries at `86805a2` (sub-launch runs) and `74fcfe7` (plus the rewind), host load 2.8-7.1 as logged in
`logs/gate-steps/step15-*`, `step18-*`, `step21-*`, `step24-*`. Correctness at every stage: op-verify 450/450 at
chain depths 32/64/128, the 96-token 27B greedy text byte-identical to `logs/shim-simple-20260914-225831.txt`, the
96-token MoE text byte-identical between flag off and on (`logs/shim-simple-20260919-085418.txt` is the flag-off
text). No crash, no fallback, no refusal at any stage after the replug.

| configuration | MoE 35B-A3B tg128 | dense 27B tg128 | pushbuffer a flush |
|---|---|---|---|
| off (the defaults) | 145-165 (mean of 7: ~161) | 67.0-67.9 | copy engine |
| `TINYNV_DELTA_DELIVERY=1` (sub-launch runs, `86805a2`) | 89.5, 89.8 | 63.0, 65.1 | 85-95 KB |
| + `TINYNV_DELTA_REWIND=1` (`74fcfe7`) | 140.5, 151.3 | 70.6, 68.4 | 6.4-6.6 KB |
| `TINYNV_TAIL_RELEASE=1` alone | 163.7, 164.7 | 67.9, 67.7 | copy engine |
| + `TINYNV_TAIL_RELEASE=1 TINYNV_DELTA_DELIVERY=1 TINYNV_DELTA_REWIND=1` | 165.8, 158.1(±9.4), 167.2, 165.6, 167.9 | **70.8, 70.6** | 2.1-2.3 KB |

1. **Sub-launch runs alone engage on every flush (1223/1223 dense, 938/938 MoE) and lose 44% on MoE, 4% on dense.**
   Each flush's runs still carried ~70 KB of a ~200 KB envelope; ~90 KB of pushbuffer a flush is ~380 µs of
   processor writes across the link at the ~4.2 µs/KB of §4d, on every flush, against the one boundary handoff
   (~350 µs a token) it removes. The gap sweep (`step16-*`: gap 0/8/32/128/512) showed the cost tracks bytes, not
   patch count - 298 to 1504 patches a flush at ~100 KB all read 83-85 t/s - so no cheaper per-patch primitive would
   have helped. §4f's "why there is no fifth lever" arithmetic stood exactly as written.
2. **Why 35% and not the "2.7% a token" the lever was built on.** The ring rewinds at a chain boundary, so what was
   last delivered at a launch's address is a *different* launch of the same kernel (another layer's pointers and
   strides). The 2.7% (`26b0562`) was a launch-to-launch measurement read as token-to-token.
3. **`TINYNV_DELTA_REWIND=1`** (`74fcfe7`): at the first launch after a standstill the descriptor region is declared
   full so the next placement comes round through `arena()`'s own wrap path. Launch k of every token then lands where
   launch k of the last one did. Footprint 85 KB -> 6.6 KB a flush (prediction on the record in the commit: under 15).
   Dense became a win (+2.6 t/s); MoE still ~7% behind, and almost all of the 6.6 KB was one patch per launch: the
   release value every descriptor carries, which advances each token.
4. **`TINYNV_TAIL_RELEASE=1`** (an existing knob: only the chain's tail releases the timeline) removes that patch:
   2.1-2.8 KB a flush, ~24 patches - the genuine per-token parameter changes. **Result: dense 67.8 -> 70.7 (+4.4%,
   ±0.2 on both sides; 70.81 is the highest tg128 this project has recorded), MoE ~161 -> ~166 (+2 to +3%, tail
   release alone being about +2% of it).**

**Recommendation, not done: flip the three knobs on by default** - Antonio's call, because it changes the shipping
behaviour behind the README table and three things have not been exercised with them: `llama-server` with several
slots under `tools/soak.sh` (standstills happen differently with slots in flight; the region has to hold one
"step" of every slot between rewinds - 2.5 MB a dense token against 64 MB is fine, but measure), speculative decode
(`spec`), and image generation (`sd`). Tail release also costs hang diagnosability: a chain that stalls cannot be
located from the timeline (`exec.c`'s wait-failure report says so itself and names the re-run without the knob).
Until then the README numbers stay as they are: they describe the defaults, and the defaults did not move.

**State at the end of the card session:** lock free, card idle warm, preflight clean at the last step. `main` at the
commit carrying this section, pushed. Binaries `llama-simple/test-backend-ops/llama-bench-null` at `74fcfe7`.

## Server soak with the three knobs, 09:20-09:25: clean

`TINYNV_TAIL_RELEASE=1 TINYNV_DELTA_DELIVERY=1 TINYNV_DELTA_REWIND=1 sh tools/serve.sh start` (27B, MTP draft on,
one slot, `llama-server-null` relinked at `74fcfe7`), then `PROBE_EVERY=4 sh tools/soak.sh 0.08 128`
(`logs/soak-20260919-092047.log`): **236 requests, 0 errors, server alive throughout, 59 greedy probes all
byte-identical to the first, last-window mean 125.9 tok/s with the draft** (the README's served-with-MTP figure is
~124). The driver's exit line from the server (`logs/serve-20260919-092014.log`): 481,210 flushes all patched at 2.0 KB
a flush, 0 fell back, 252,833 rewinds - MTP's draft-verify loop stands still several times a token, and the rewinds'
per-piece waits cost 25.67 ms in total over five minutes. So of the three things named above as untested, the server
with speculative decode is now covered for one slot; several slots in flight, and `sd`, are not.

Also fixed on the way: `tools/serve.sh stop` looked for the lock at the checkout's own `.gpu-lock`, but with `env.sh`
sourced the lock lives at the shared path (`GPU_LOCK`), so a stop would have left the shared lock held. It now checks
`${GPU_LOCK:-$R/.gpu-lock}`; the release was seen to land.

**Final state:** lock free, card idle warm, preflight VERDICT OK (dext pid 33020 unchanged since the replug). `main`
pushed. Nothing in flight.

## The rest of the pre-flip list, 09:28-09:31: image generation and two slots, both clean

- **Z-Image-Turbo, 8 steps, 1024², seed 42** (`step25-sd-zimage-*`, `sd-cli-null` relinked at `74fcfe7`): the PNG
  is byte-identical off vs on (and across all three runs). Warm timings: off 9.98 s total / 8.38 s sampling, on
  9.79 s / 8.19 s. With the knobs on the rewinds waited 787 ms in total on pieces still in use (94 standstills - a
  diffusion step's standstill pattern is not a decode token's), and the run was still not slower.
- **Two slots with concurrent load** (`PARALLEL=2`, `tools/loadgen.py 2 8 128`, the scratch script's output is in this
  session's record): greedy probe before and after the load identical off vs on, and identical to the one-slot soak's
  hash (`d9839d066907e60d`); 16/16 requests both ways; aggregate 123.4 tok/s off vs 123.9 on; draft accept 46% both.
  Footprint 5.4 KB a flush with two sequences' changes per step, 74 ms of rewind waits over 18,108 standstills.

So everything named above as untested has now run with `TINYNV_TAIL_RELEASE=1 TINYNV_DELTA_DELIVERY=1
TINYNV_DELTA_REWIND=1`: op-verify x3 depths, dense and MoE greedy texts, the server with MTP for five minutes, two
slots under load, and image generation - all byte-identical to the defaults where a byte-compare exists, dense tg128
+4.4%, MoE +2-3%, the rest within noise. **What is left before flipping the defaults is only the decision**: the three
parsers in `exec.c` (`tinynv_exec_tail_release`, the `TINYNV_DELTA_DELIVERY` block, the `TINYNV_DELTA_REWIND` block)
and the startup lines' "the default"/"was asked for" wording, then the README table re-measured under the gate.
The one cost that does not go away is tail release's: a stalled chain cannot be located from the timeline, and the
wait-failure report names the re-run without the knob. Antonio's call.

Final state: lock free, card idle warm, preflight VERDICT OK at 09:3x. `main` pushed.

## The sd sweep, 09:35-09:55: every model five seeds, off vs on, and one pre-existing fault found

Antonio asked for all the sd models, ~5 images each. `models/sd` holds three generatable models (the other three files
are the FLUX VAE, the LCM LoRA and Z-Image's Qwen3-4B encoder). Five prompts x seeds 42..46, each seed generated with
the three knobs off then on through the protocol runner, PNGs byte-compared per pair; `logs/gate-steps/sd-sweep-
20260919.txt` has every line, `logs/gate-steps/sd-<label>-s<seed>-<mode>.log` each step.

| model | settings | off, mean of 5 (total / sampling) | on, mean of 5 | PNG pairs |
|---|---|---|---|---|
| SDXL Turbo fp16 | 512², 4 steps, cfg 1 | 2.67 s / 1.44 s | 2.70 s / 1.46 s | 5/5 identical |
| SD 1.5 (emaonly) | 512², 20 steps, cfg 7 | 3.71 s / 3.16 s | 3.72 s / 3.16 s | 5/5 identical |
| Z-Image-Turbo Q8 + FLUX VAE + Qwen3-4B | 1024², 8 steps, cfg 1 | 9.94 s / 8.29 s | 10.02 s / 8.36 s | 5/5 identical |
| SD 1.5 + LCM LoRA | 512², 4 steps, cfg 1, `--sampling-method lcm` | **faulted, 5/5** | **faulted, 5/5** | none |

Images looked at (fox, strawberry, astronaut, lighthouse, cabin): coherent and on-prompt on all three models. Image
generation is neutral to the knobs, as the single pair earlier suggested.

**The LCM LoRA path faults the card, with or without the knobs - a pre-existing bug, not this session's.** Every run:
stable-diffusion.cpp logs `apply lora immediately`, then `(0 / 834) LoRA tensors have been applied` (no tensor name
matched), then gsp-rm reports an MMU fault (event 4101, a 32-byte zero payload), the debugger sees no compute-channel
fault, and the copy channel is parked on a 21-dword copy batch that never releases; the driver aborts at its 30 s
wait (`exit 134`), the next open recovers the card, preflight reads clean after each. Ten such runs happened
(09:40-09:51) because the sweep script ran on past failures - a process mistake, fixed in the script the same hour
(it now stops at the first crash or refusal), and one to carry forward: **a sweep never continues past a fault.**
The likely shape is a copy issued for a LoRA tensor that was never allocated because nothing matched; it is a
stable-diffusion.cpp-through-the-shim bug worth its own investigation, offline first (the null device with the same
command line, to see what the copy is), and it does not need the card to start.

## Handover, ~09:57: the card is A's until Antonio says otherwise

Antonio, mid-session: "when you are all done with the card let A know I'm going to give him the card for awhile after
you finish up. dont take the card back til i get back and talk to both of you." Sent to A (session `egpu-69`) by
session message with the state above (lock free, idle warm, preflight OK, the LoRA fault warning). **From here this
session takes no card step - no `nv_shim_step.sh`, no `serve.sh start`, no lock - until Antonio is back and has
talked to both.** Read-only preflight remains fine. Recorded in memory (`card-handed-to-a-2026-09-19`).

---

# 10:45-11:15: README's second known limit closed - the firmware reservation derived and checked

Antonio gave the card back ("card is yours go ahead and start working") and the agent's plan
(`docs/driver/wpr2-reservation-plan.md`) was implemented as written, offline first, then measured. `30751d1` and the
docs commit after it.

**The change.** `fw_layout.h` names every size the driver hands the firmware (the WPR meta in `gsp.c`, the FRTS
placement in `flcn.c`), sums what they can occupy (~240 MB) and static-asserts that the 256 MB hold-back covers it;
`mmu.c` holds back `TINYNV_FW_RESERVE_TOP` instead of the flat 64 MB; `tinynv_mm_check_fw_carveout` (pure) refuses
if the manager's top is above WPR2's base minus the non-WPR heap, refuses nonsense registers, and reports hidden
registers as unverified; `device_boot` reads `NV_PFB_PRI_MMU_WPR2_ADDR_LO/HI` after the firmware has booted, prints one
line and refuses the open on a mismatch (`TINYNV_FW_RESERVE_CHECK=0` downgrades to the line); the GSP-RM image and the
bootloader are bounded at load; `total_mem` reports the manager's size. The page-table reservation is the same 64 MB
for any hold-back under 351 MB on this card, so `pa.base` stays `0x4200000` and the recorded boot replays with zero
divergences - the property that made this a small change rather than a re-recording.

**Offline:** `test_mm` pins the base and the top and drives the check with the expected layout, an overlap, hidden
registers, two misreads and the exact boundary from both sides; `test_dev` pins the same two numbers against the
recorded card; `test_headers` pins the heap size to the vendor's formula term by term (needed
`generated/rmconfig.h` on the include path) and the non-WPR heap to the GB20x value. Three negative controls seen to
fail first: a 64 MB hold-back fails the static assert at compile time; 512 MB moves `pa.base` to `0x4100000` and trips
the guard; dropping the non-WPR subtraction fails the boundary case. Full suite green after a clean rebuild.

**On the card, every number as predicted before the run:**
- `test_hw_wpr` twice (cold and warm open, identical): WPR2 `0x7e7e20000..0x7f4901000`, 202.9 MB, starting 224.9 MB
  below the top; the manager stops at `0x7e5f00000`, 31.1 MB below WPR2 and 29.0 MB below the non-WPR heap. The old
  top was 161 MB inside the region, the README's number exactly. Now recorded in `libtinynv-design.md` §4f-ter.
- `test_hw_fill` (new): 31 pieces of 1 GiB, 31,744 MB, the driver's pools and tables holding the other 541 MB of
  32,285; every piece's first and last page written through the engine and read back intact; GSP-RM's free heap
  85,487,616 bytes before and after; sensors answering. (Its first run failed only on my expectation of the refusal's
  wording: a full card refuses through the chunked path, "cannot be built from the chunk sizes available", not the
  region's message. Fixed, rerun, all checks passed.)
- The gate at the defaults: op-verify 450/450 at depths 32/64/128, the 96-token text byte-identical to the 09-14
  reference, tg128 dense 67.96 ± 0.05 and MoE 161.44 ± 1.09 - unchanged, as a reservation change must be.
  `llama.cpp` now prints "Total VRAM: 32285 MiB".

**Left open, named in the README bullet:** `cudaMemGetInfo` still reports free = total; removing that needs a
live-bytes counter in the manager. `gspFwHeapSize` stays at the vendor's 135 MB (raising it is possible now, and the
plan's §6 says why it is not wanted). The hardware probes carry no build id (nothing in them references it) - the
runner's "unknown" for a probe is expected, not a stale binary.

State: lock free, card idle warm, preflight OK. `main` pushed.

---

# 11:20-11:35: back on the disaggregated-inference feature - the router's cost model derived, window 4 run

Antonio: the feature to be working on is the disaggregated inference one (prefill on the 5090, decode on Metal; the
re-enumerations during its Metal prefills were the "fault" that turned out to be the power adapter). Its state doc
(`docs/disagg-inference-state.md`) has the full account; item 1 of its next-list was the router's cost model.

- `11cf4c6`: the card's cost is expert GiB over link rate plus a first-prefill cost per GiB, both read off the
  GGUF's header (`expert_layout()`) and `--ncpumoe`; `tools/disagg-router-model-test.py` reproduces all six recorded
  prefills of windows 2 and 3 within 0.1 s. Coder-Next's experts are 43.69 GiB (the notes' 43.50 was a hand estimate).
- Window 4 (state doc, new section): Coder-Next at `NCPUMOE=30` on this tree's driver `0c15edc` - possible here only
  because the morning's WPR reservation fix made residency safe on it - prompt A first 10.2 s (predicted 10.3),
  chat-6k 5.7 (6.3), prompt B 9.0 (9.2), every point inside its registered band; the first-prefill term landed to 0.1 s.
  op-verify 450/450 after. Logs `logs/disagg/serve/w4-20260919-113019-{card,metal,router}.log`.
- Still open on that list: the reversed-order rerun (2), prompts past 24,576 tokens on the current driver (3), a
  dense model through the gate (4), the vision projector (12).

State: lock free, card idle warm, preflight OK. `main` pushed.

---

# 11:40-12:00: the three knobs become the defaults (`4714f86`); then a DART fault of my own making

**Antonio: "turn delta diffing on please."** Done as the validated combination, since delivery alone is the measured
44% MoE loss: `TINYNV_DELTA_DELIVERY`, `TINYNV_DELTA_REWIND` and `TINYNV_TAIL_RELEASE` are on unless `=0`; the
parsers are functions (`tinynv_exec_delta_delivery/rewind`, `tinynv_exec_tail_release`) and `test_exec_mode` pins
all three defaults, both spellings and the rewind-follows-delivery rule (a planted flip of the tail-release default
failed 4 checks; restored, 0). Every startup line says "the default" or "was asked for"; delivery without the rewind
warns that it is the measured loss; the per-launch release is now the `TINYNV_TAIL_RELEASE=0` diagnostic and the
wait-failure report says so.

**Gate at the new defaults, empty environment, driver `4714f86` (11:50-11:54):** op-verify 450/450 at depths
32/64/128; the 96-token 27B text byte-identical to the 09-14 reference, 1561 flushes patched at 2.7 KB a flush, 0
fell back; the opt-out path (`TINYNV_DELTA_DELIVERY=0 TINYNV_TAIL_RELEASE=0`) also 450/450 and byte-identical, with
its own startup lines. **tg128: dense 68.5 / 68.8; MoE 138.9 / 140.1.** Those MoE numbers are 16% under the 161.4 the
same code paths read at 11:14 and the ~166 the three knobs read at 09:11-09:17; an interleaved pair right after
(opt-out 136.4 / 135.4 vs defaults 140.7 / 141.7) shows the defaults still +3-4% ahead in the same minutes but the
WHOLE card slower on both paths - so not the flip, and it began before the fault below. Unexplained: nothing
between 11:15 and 11:50 changed the driver's launch path (`30751d1` was already in the 11:14 gate; `4714f86` only
flips defaults), the host was quiet (no build, no indexer; system daemons at 10-16% each in `top`), the
Coder-Next window (11:30-11:34, 16 GiB of experts resident) is the only card use in between. The card's own clocks
and temperature would say - and are exactly what I then tried to read, badly.

**The fault, ~11:56, mine.** `cuda-shim/build/shim/nv/tinynv-smi 2>&1 | head -12`: with nobody holding the card the
smi OPENS it (its output began with the boot's engine table, which is a card open, not a read of a published file),
and `head` closed the pipe after twelve lines, so the smi died of SIGPIPE mid-session - the same "killing the job
killed the card" shape as this morning's link-down, this time leaving `pci-dart-error-data` latched (1,220 bytes,
~30 same-shaped records with zero addresses). Link up, dext pid 33020 unchanged, nothing on the card, lock free;
preflight VERDICT ABORT [new DART error data]. No card action since. Per the standing rule a latched DART fault is
Antonio's own look, not an autonomous clear: `tools/dart-stale.sh "<reason>"` snapshots it so preflight reads
"stale" and proceeds, if he judges the card usable; a physical replug re-enumerates and drops it.

Two rules from this: **`tinynv-smi` is a card step whenever nobody holds the lock** (run it through the runner, or
only while a server of ours holds the card), and **never pipe a card process into anything that can close the pipe
early** - `head`, `grep -m`, a `cut` that exits - because SIGPIPE is a kill.

**State:** `4714f86` committed, not yet pushed at the time of writing (pushed with the docs commit after this). The
chain-replay research agent Antonio asked for is still out; its report goes to `docs/driver/` beside the WPR2 plan.

## 12:00-12:11: fresh replug (two tries), everything run again on the new defaults

Antonio unplugged; the first plug (12:00:56) enumerated a dext instance that exited within seconds and left the 5090
off the PCI bus (nub gone, "IOPCIResourced missing", box still on Thunderbolt) - a too-quick replug's signature. The
second plug came up at 12:06:18 (dext pid 65787); preflight read link up, DART absent, new instance; cleared under the
standing rule, second preflight OK. The old TinyGPU server had died with the unplug, so the runner started a fresh one.

**Gate at the new defaults, empty environment, driver `4714f86`, fresh card (`step32-*`):** op-verify 450/450 at
depths 32/64/128; the 96-token text byte-identical. tg128: dense 68.7 / 66.7 (the second at host load 11.9), MoE
142.7 / 141.6; interleaved opt-out vs defaults: 136.3 / 135.5 vs **143.7 / 142.0** - the defaults +5% in the same
minutes, both paths still ~16% under the 11:14 reading (161.4 opt-out).

**Diagnosis, from readings rather than a guess:** the card is cool and at full clock - its own sensor file from the
first step of this gate (`${TMPDIR}tinynv-sensors`, 12:07) reads 45.7 C, 61 W idle, graphics 2407 MHz, pstate 1,
throttle none - so the drop is not thermal or a clock state, and a replug did not change it. The host is the suspect:
load average 4-12 through these runs against 2.5-4 this morning; `top` shows system daemons alone over a core
(WindowServer 33%, biomesyncd 29%, BiomeAgent 20%, tccd 15%, launchservicesd 14%, trustd/loginwindow 10% each), no
build, no model server of ours left running. MoE decode issues ~1,600 launches a token with ~1 us of host work each
against a 6 ms token, so it is the workload most exposed to the host being descheduled (README: "MoE numbers move
more with host load"); dense moved 3%, MoE 16%, and the on/off ratio is unchanged. The definitive check is the same
interleaved pair on a quiet host (the other sessions idle) - the README's numbers stand on the 09:1x pairs at load
2.5-4 until then, and every number here carries its load.

## 12:15-12:40: the four cheap reads for chain replay - done; the verdict is "do not build"

Antonio's go. Results in `docs/driver/chain-replay-plan.md` (last section) and `libtinynv-design.md` §4h:
(1) steady-state descriptors differ token to token in the tail release only, after ~4 settling tokens;
(2) short first chain: no change; (3) depth: 128 is the optimum, 1024 costs 14-17%, op-verify passes at every depth
up to 1024 (ceiling raised to 1024, default unchanged); (4) the card boosts fully (2,860 MHz, pstate 1, 154-306 W,
no throttling) and is busy 66% (MoE) / 78% (dense) during a decode. Also from (4): Antonio's fan observation led to
the sensor publisher being wired into the flush path - until then no decode had ever published clocks or power.
The 20-minute 8-slot soak at depth 1024 was not run: with deeper chains a loss and replay off the table, its purpose
(the long-chain regime for a 2,000-link replay) is moot; op-verify at 1024 stands as the regime's safety read.

**Next lever, per the study's own fallback:** the per-QMD tail - the five cache invalidates and the L1_SYSMEMBAR
membar every descriptor carries (`qmd.c:170-183`) - as an A/B on non-releasing descriptors with op-verify at three
depths and the byte-compare. Not started.

State: lock free, card idle warm, preflight OK. `main` pushed.

## 12:45-13:03: the per-QMD tail A/B - the invalidates are load-bearing, the barrier costs the dense 3%

Antonio: "let's do it. you have the card." Two knobs (`f96ac28`, off by default, pinned by `test_exec_mode`):
`TINYNV_QMD_MEMBAR=sys|gpu|none` (the barrier scope at the end of every NON-releasing descriptor; releasing ones keep
system scope, restored at the flush) and `TINYNV_QMD_INVALIDATE=all|cb0|none` (the five per-launch cache invalidates).

| configuration | op-verify | 96-token dense text | MoE tg128 (x2) | dense tg128 (x2) |
|---|---|---|---|---|
| default (oracle's tail) | 450/450 | identical | 139.3 / 138.7 | 68.1 / 68.7 |
| membar=gpu | 450/450 | identical | 137.6 / 140.4 | 68.3 / 68.4 |
| **membar=none** | 450/450 at 32/64/128 | identical; MoE text identical too | 140.1 / 139.8 | **70.7 / 70.3** |
| invalidate=cb0 | 450/450 | **DIFFERS** | - | - |
| invalidate=none | 450/450 | **DIFFERS** | - | - |

- **The texture/shader cache invalidates are load-bearing** for a chained decode: op-verify cannot see it (each of
  its tests syncs, and the per-chain command-stream invalidate covers a chain boundary), the byte-compare can. Retired.
- **The barrier's cost is the barrier, not its scope**: gpu scope buys nothing; none buys the dense decode +3% and
  the MoE nothing. `membar=none` then passed the pre-flip list: op-verify at three depths, both texts byte-identical,
  a five-minute `llama-server` + MTP soak (`logs/soak-20260919-125754.log`: 232 requests, 0 errors, 0 mismatches,
  124.5 tok/s). Two slots under load and the image models have not run with it.
- **Recommendation, Antonio's call:** flip `TINYNV_QMD_MEMBAR=none` on by default for +3% dense; the invalidates stay.

**Where that leaves the MoE.** After today: delta delivery (+2-3%), the depth optimum confirmed, no boundary-build
term, no seam term, no tail-barrier term - and the MoE still at ~140-166 against native's 245 with the engine busy
66% of a token at full clock. What remains is inside kernel execution and the ~1 us inter-kernel dispatch that native
has too; nothing the descriptor or the delivery does. The next questions are kernel-level (the MoE's mul_mat_id path
and its launch count against native's, which graphs collapse), not driver-level, and they are for another day.

State: lock free, card idle warm, preflight OK. `main` pushed.

## 13:10: the MoE deep dive is the standing plan; option 1 started

`docs/driver/moe-next-steps.md` (`8c3136c`) ranks six performance options for MoE decode; Antonio: "let's start with 1.
please put this list into the record for the next agent." The list with status lives in memory as `moe-next-steps-plan`
and here: 1 move the remaining per-token transfers off the copy engine (IN PROGRESS), 2 per-kernel timing from the GPU
clock plus a native Nsight trace, 3 update the vendored llama.cpp for its fused MoE kernels, 4 speculative decoding
with the 0.8B draft model, 5 a narrower set of per-launch cache invalidates, 6 multi-slot serving. Already measured
and settled: launch-chain replay, chain depth, the short first chain, the barrier scope, removing the invalidates.

Option 1, step 1 done (`step43-trace-moe.log`): per decode token the shim issues 1,634 launches, 23 synchronisations,
seven tiny uploads (4, 4, 8, 8, 16, 512 and 8,192 bytes) and one 993,280-byte logits download. Only the 8,192-byte
upload and the download still use the copy engine; the rest already ride in the pushbuffer.

## 13:15-13:27: option 1 measured - the last per-token upload off the copy engine is +2.3% MoE, the download move is nothing

Per token the shim still sent one 8,192-byte upload (above the 4,096-byte inline cap) and the 993 KB logits download
through the copy engine (`step43-trace-moe.log`). The cap became a runtime knob with the per-call ceiling raised to
the method's real 32,764 bytes (commit after `8c2fbbc`); the download already had `TINYNV_DOWNLOAD_VIA_COMPUTE=1`.
Interleaved tg128, two rounds, host load 6-9 (`step45-*`): MoE default 136.0/137.7, inline 139.3/140.6,
download-via-compute 135.1/136.2, both 140.8/140.2; dense 68.3/68.2, 69.3/69.3, 68.1/67.9, 69.4/69.1. op-verify
450/450 and the 96-token text identical for all three (`step44-*`). **Inlining the 8 KB upload: +2.3% MoE, +1.6%
dense; the download move: nothing.** The copy engine's remaining per-token work was worth ~0.15 ms, the same figure
§4f found when inline uploads first landed - not the 0.35 ms handoff hoped for; that switch is evidently already off
the critical path since the delta defaults. **Candidate default, Antonio's call:** `TINYNV_INLINE_MAX=32764` with the
held list sized 16 x 2 KB (today 16 x 512 B, which an 8 KB upload alone fills). Option 1 is done; option 2 (the
per-kernel clock profile) is next on the list.

## 13:30-13:47: both free percentages are the defaults (`f7a65d6`), gated, image models included

Antonio: "if it doesn't break anything no reason not to take free percentages" and "why not run stable diffusion to
see if it hurts or helps there too." Flipped: `TINYNV_INLINE_MAX` default 32,764 with the held list 16 x 2 KB, and
`TINYNV_QMD_MEMBAR` default none (releasing descriptors keep system scope; `=sys` is the oracle's). The mode test pins
both (111 checks; the barrier default planted back failed 3). Gate at the new defaults, empty environment
(`step46-*` to `step48-*`, `soak-20260919-134201.log`): op-verify 450/450 at 32/64/128; 27B text identical to the
09-14 reference, MoE text identical to this morning's; interleaved old vs new: MoE 140.3/140.5 -> 142.5/145.0, dense
70.9/70.6 -> 71.7/71.3; SDXL Turbo 2.79 s / 2.79 s, SD 1.5 3.74 / 3.74, Z-Image sampling 8.64 / 8.63 with all three
PNGs byte-identical; server + MTP soak 225 requests, 0 errors, 0 mismatches. README table and known-limits bullet
updated. State: lock free, card idle warm, preflight OK, `main` pushed.

## 14:15: option 2 of the MoE plan - a per-kernel profile from the engine's clock (`f562d0b`)

Antonio: "lets go on to 2." `TINYNV_KERNEL_PROFILE=1`: every descriptor releases a four-word report (its timeline
value, then the clock) into a ring slot of its own in host memory; the chain's tail is stamped by a command-stream
release with wait-for-idle instead of by its descriptor, so nothing rests on the second release slot. Slots are read
in order once the timeline has passed them; the interval between stamps goes to the later kernel (its execution plus
the dispatch gap before it, which a completion stamp cannot separate); the token boundary is kept apart; the first
four windows are not counted. Mode test 136/136, full card-free suite green, binaries relinked at `f562d0b`.

**Prediction, on record before the run (MoE 35B-A3B, `simple` n=96, host load ~5):** the greedy text is
byte-identical to this morning's reference (a stamp changes nothing a kernel computes); ~97-99 windows, ~1,634
stamped launches a window; kernels + boundary per window add up to the token period under the profile's own cost
(6.5-7.5 ms; the profile itself is worth 1-3%); the small kernels show the floor at ~3-4 us each (dispatch gap +
a trivial kernel), and the split decides the next step - a few large kernels dominating at durations above native's
Nsight readings means "kernels run slower here", ~1,500 tiny kernels at the floor means "the gap is the gaps".
Refusal readings: missing stamps above ~1% (the in-order landing assumption is wrong), clocks backwards (not one
clock), unstamped launches (ring too small), a tail missing on every chain (the command-stream stamp did not fire).

**Measured 14:11-14:17 (steps 49-53; `c95bba9` fixed the boundary flag, `8843e12` added the seam and long-interval
lines):** both greedy texts byte-identical under the profile (MoE to 13:37:11, dense to the 09-14 reference); 107
windows counted of 111, zero stamps missing, zero unstamped, zero refused, no clock backwards - the instrument
holds. The profile costs the MoE ~6% (130.6 vs 139.3 tok/s in `simple`) and the dense ~3%: the payload changes
every token, so the delta path carries ~4.6 KB a flush more.

| per window (~1.16 windows a token in `simple`) | MoE 35B-A3B | dense 27B |
|---|---|---|
| launches stamped | 1,476 | 1,795 |
| in kernels (each interval = kernel + the dispatch gap before it) | 5,381 us | 12,032 us |
| of which the chain's first intervals (the seam: engine waiting for the host's next chain) | **1,797 us in 10.8 chains** | 480 us in 13.5 chains |
| intervals >= 50 us at a chain's first launch / inside a chain | 10.3 (1,777 us) / 1.8 (302 us) | 0.5 (95 us) / 34.6 (2,743 us: the big matmuls) |
| at the boundary (engine idle + host turnaround + first kernel) | 1,240 us | 1,296 us |
| mean interval inside a chain, excluding seams | **2.45 us** | 6.5 us |
| top kernel by time | mul_mat_vec_q 38.5% (312 launches, 6.6 us mean) | mul_mat_vec_q 75.7% (409 launches, 22 us mean; the Q6_K lm_head 615 us) |
| host (launch profile, same run) | 1.28 us a launch in the driver; host busy ~95% of the decode | 1.30 us; host busy ~92% |

**Reading.** The MoE's kernels are not slow: inside a chain a launch takes 2.45 us including its dispatch gap, and a
window's in-chain time (~3.6 ms, ~4.1 ms a token) is about native's whole token (4.1 ms at 245 tok/s). The whole
gap to native is engine idle: ~1.8 ms a window at the chain seams and ~1.24 ms at the boundary. The seams say the
host builds a 128-launch chain slower than the engine runs it (host ~4.3 us a launch all layers included, of which
the driver is 1.28; engine ~2.45), so the engine drains and waits at nearly every seam. **The MoE decode is
host-bound.** The dense is the opposite: 128 launches run ~860 us and build ~550 us, seams are ~35 us, and the
kernels are the token (12 ms of 15.4 for 15.3 GiB of weights = 1.27 TB/s effective, ~71% of the card's peak); its
gap to native is the boundary. The chain-replay study's premise that the host pays ~1.05 us a launch was the
driver's share only - the true host cost is ~4.3 us a launch and it is the MoE's limit. Next: split the ~3 us above
the driver between the runtime layer and ggml-cuda/llama.cpp (`TINYCUDART_STATS=1` now times the caller between
launches, `35f17de`, one card step), then the Windows Nsight capture for the per-kernel comparison.

**14:22, step 54 (`35f17de`, MoE with `TINYCUDART_STATS=1` and both profiles, text identical):** per launch the
host spends **2.38 us between consecutive launches** (ggml-cuda's dispatch and llama.cpp above the runtime layer;
157,823 pairs) and **1.75 us inside `tinynv_launch`** (of which 1.29 us is the driver building the launch, the rest
the API layer's lock and checks): **4.1 us a launch on the host against 2.45 us on the engine.** A 128-launch chain
takes the host ~530 us to build and the engine ~314 us to run, which is the seam stall the kernel profile measured
(1.85 ms a window at 10.4 chains). Per-kernel submission cost varies with the parameter list: quantize_q8_1 0.9 us,
mul_mat_vec_q 1.5, k_bin_bcast 2.1-3.6, rope_multi 8.5 us (17 a window, ~145 us - worth a look on its own).

**What this changes.** The chain-replay study (`docs/driver/chain-replay-plan.md`) priced replay from the engine's
side and assumed the host paid ~1.05 us a launch; the host pays 4.1, and the MoE's whole gap to native is the engine
waiting for it (seams 1.85 ms + boundary 1.24 ms of a 6.6 ms window). A CUDA-graph subset in the runtime layer -
capture records each launch's kernel, parameters and shape; replay re-issues them to the driver without ggml's
2.38 us a launch - would put the host at ~1.75 us a launch, under the engine's 2.45, and the MoE window at roughly
its in-chain time plus a shorter boundary: ~4.5-4.8 ms against 6.6 today (+35-45%). The dense would gain only at
the boundary (~1.3 ms of 15.4). That is Antonio's call; the plan doc's option list is updated. The Windows Nsight
capture is still worth having for the per-kernel comparison (the mmvq means here vs native's) but no longer decides
anything: the in-chain time already matches native's whole token.

## 14:40: the ggml-cuda host halves were compiled at -O0 - rebuilt at -O2 without the Linux box

Reading the build for the graph work: `cuda-shim/build/tinycc` (untracked, `build/` is ignored) host-compiles every
ggml-cuda TU with no `-O` flag at all, so the dispatcher that costs 2.38 us a launch above the runtime layer ran at
-O0. The Linux nvcc box is not in this environment, but the device halves are embedded verbatim in the archive's
objects: extracted all 183 `__NV_CUDA,__nv_fatbin` sections into `/tmp/tinycc_<tu>.fatbin` (the five TUs without one
are host-only dispatchers with nothing to register; placeholders), added `TINYCC_HOST_OPT` (default -O2) to tinycc
and `DEFS_EXTRA` to build-ggml-cuda.sh (which now keeps `libggml-cuda.prev.a`), rebuilt with
`TINYCC_REUSE_FATBIN=1`: 188/188 in 21 s, every new object's fatbin byte-identical to the old, ggml-cuda.o's host
code 698 -> 255 KB. Old binaries kept for the A/B.

**Prediction:** texts byte-identical; the caller's time between launches falls from 2.38 us to ~1.0-1.5 (llama.cpp's
own share stays); host cost a launch ~4.1 -> ~3.0 us; MoE seams shrink from 1.85 ms a window towards ~0.5-1.0;
MoE tg128 +10-15% interleaved; dense within noise (+1% at most, the boundary).

**Measured 14:35-14:53 (steps 55-58).** `simple` under both profiles, -O2 build, texts byte-identical: MoE 130.6 ->
169.1 tok/s; the caller's time between launches 2.38 -> 0.42 us a launch, inside `tinynv_launch` 1.74 (unchanged);
chain seams 1.85 -> 0.41 ms a window; in-kernel time 5.38 -> 3.84 ms a window. Dense 66.0 -> 67.1. **Interleaved
tg128, old binaries vs new, two rounds, host load 6-7:** MoE 139.7/142.8 -> **214.4/217.4 (+53%)**; dense
71.5/70.7 -> **74.0/73.9 (+4%)**. Native: 245 and 75.3. **Gate on the -O2 build** (every null binary relinked at
`8843e12`): op-verify 450/450 at 32/64/128; SDXL Turbo 2.75 s, SD 1.5 3.71 s, Z-Image 16.6 s (sampling 8.40) with
all three PNGs byte-identical to the 13:3x gate's and the morning sweep's; server + MTP soak 232 requests, 0 errors,
58 probes, 0 mismatches, 125.1 tok/s (120.1 at 13:42). `tools/serve.sh stop` left its own lock on file when run from
a shell without `GPU_LOCK` (it looked at the checkout's `.gpu-lock`); fixed to resolve the path as gpu-lock.sh does;
the lock was released by hand, preflight OK, card free.

**What is left, re-read after -O2.** Host cost a launch is now ~2.2 us (0.42 caller + 1.74 driver entry) against
the engine's 2.45, so the seams are at their floor (~40 us a chain) and a CUDA-graph subset would buy only the
residual - it drops to a small item. The MoE token is now ~4.45 ms in kernels (about native's whole token) plus
~1.2-1.4 ms at the boundary; the dense is kernel-bound at ~1.27 TB/s plus the same boundary. The boundary is the
lever left on both: what the host does between the last kernel of a token and the first of the next (sampling, the
ggml graph build and scheduler, the first chain), and how much of that native also pays - which is what the Windows
Nsight capture answers (the gap between tokens in its trace). The driver's own 1.74 us a launch is the other.

**Reproducing the build (the scripts under `cuda-shim/build/` are untracked - `build/` is ignored):** the device
fatbins are recovered from the current archive's objects (`llvm-ar x`, `llvm-objcopy --dump-section
__NV_CUDA,__nv_fatbin=/tmp/tinycc_<tu>.fatbin`; the five TUs without one - fattn-tile, mmf, mmq, set, top-k - are
host-only and take a non-empty placeholder), then `TINYCC_REUSE_FATBIN=1 TINYCC_HOST=unused JOBS=8 sh
build/build-ggml-cuda.sh` rebuilds the host halves at -O2 in ~20 s and keeps the previous archive as
`libggml-cuda.prev.a`; relink with `build/link-null.sh` / `link-llama-bench.sh`. Whether those scripts should be
tracked is Antonio's call.

## 15:00: the Pop!_OS 3090 box and Docker are available - a native reference trace I can drive myself

Antonio: "you can use docker to rerun anything you need linux for or you can use the 3090 box as well" and "ask A
about the 3090, he's working there right now." Peer sessions exist again (ListAgents): A = `egpu-69` coordinates the
3090 box, B = `egpu-26` is the libtinynv/VM-driver track. Access is `ssh -i ~/.ssh/gpupod_ed25519
davinchy@192.168.1.110` (A also authorised this Mac's own key). The box: Pop!_OS 24.04, RTX 3090 on the open 580.126
module, CUDA 13.0, nsys 2025.3, 48 threads, 125 GB RAM, root 90% full (large files under /mnt/4tb, exFAT). Rules
agreed with A: one process on the 3090 at a time, announce each GPU run to A before and after, ack A's
module-unloading runs. Docker here: `~/.local/bin/nvcc` runs nvcc 12.8 in an arm64 container (`cuda-nvcc:12.8`).

Set up there: `~/egpu-mac-ref/llama.cpp` = the vendored b906d25 tree (tar over ssh; `--exclude models` had dropped
`tools/mtmd/models`, and `build*` had dropped `common/build-info.cpp.in` - both recopied), a user-local cmake, the
CUDA build for sm_86 (`llama-simple`, `llama-bench`); both GGUFs copying to `/mnt/4tb/models/` (the checkout's
`models/*.gguf` are symlinks into the shared tree: `rsync -L`); `native-ref.sh` (nsys profile of a 96-token greedy
decode per model, graphs on and off, plus tg128) and `native-gaps.py` (per-token kernel span, gaps, boundary and a
per-kernel table from the sqlite export, built to line up with the shim's TINYNV_KERNEL_PROFILE report). The 3090 is
not a 5090 - kernel times scale with its 936 GB/s - so the comparison is the token boundary, the launch count, the
per-launch gap, and what graphs buy natively, not absolute kernel times.

**15:15, while the box builds: the short first chain again, now that the host is not the bottleneck.** This morning
`TINYNV_SHORT_FIRST_CHAIN=8` measured no change - the engine was waiting for the host at every seam, so handing it a
short first chain early bought nothing. With the host at ~2.2 us a launch the boundary (1.2 ms a window) holds the
first chain's build (~128 x 2.2 = 280 us) before the engine can start. **Prediction:** MoE tg128 +2-5% interleaved
(up to ~250 us of the 5.1 ms window), dense within noise. Refusal reading: no change again means the boundary is
not the first chain's build but the host's own turnaround (sampling, ggml graph build, scheduler).
**Measured 15:09-15:10 (interleaved, load 6-7):** default 215.0 / 203.3 (the second at ±14, a noisy run) vs
`TINYNV_SHORT_FIRST_CHAIN=8` 208.3 / 210.9 - no change, the refusal reading. The boundary is the host's own
turnaround between tokens, not the first chain's build; the native trace's boundary is the number to hold it against.

## 15:13: the boundary is not the host - `TINYCUDART_STATS` now times the turnaround after a wait

Added to the runtime layer's stats: from the first synchronisation after a token's last launch (the logits read
back) to the next launch, whatever copies happen between. MoE `simple`, both profiles on, text identical: **the host
takes 161 us (92 tokens, worst 214)** to sample, build the next ggml graph, schedule it, upload the inputs and issue
the first launch. The engine's boundary in the same run is ~980 us a window and the launch profile's refill gap
1,081 us, of which 1,077 us "before the engine reached the batch" and 3 us at its acquire; the copy engine finished
the logits 152 us after the last kernel. So of the ~1 ms the engine idles at a token boundary, the host's own work is
~0.16 ms and the first chain's build ~0.27 ms (and a short first chain measured nothing at 15:09); some ~0.5 ms is
between the batch being handed over and the engine reaching it - the shape of the copy-to-compute runlist switch
that design doc SS4f measured at ~0.35-0.7 ms, which the logits download on the copy engine forces every token.
**Prediction:** `TINYNV_DOWNLOAD_VIA_COMPUTE=1` (the logits served by the shim's own copy kernel on the compute
queue; measured "nothing" this morning while the seams dominated) now takes 0.3-0.5 ms off the window: MoE tg128
+6-10% interleaved, dense +2-3%. Refusal reading: no change means the pickup latency is the engine's own wake-up,
not the runlist switch.
**Measured 15:15-15:16 (interleaved, load 6-7):** default 219.8 / 221.9 vs `TINYNV_DOWNLOAD_VIA_COMPUTE=1`
220.7 / 214.9 - no change: the refusal reading. Neither the first chain's build nor the copy-engine switch is the
~0.5 ms between the batch being handed over and the engine reaching it; what remains is the engine's own pickup after
it has gone idle (mid-token seams, where it never idles long, are ~40 us; the boundary, after ~0.3-0.5 ms idle, costs
~1 ms). The lever that follows is to keep the engine awake across the boundary: a small kernel launched after the
logits download that polls a host flag (with a clock64 timeout as the watchdog) and exits when the host is about to
hand over the next chain, so the first chain's acquire is satisfied at once and nothing waits on the spin. Needs a
cubin (nvcc on the 3090 box or the Docker nvcc), a flag in GPU-visible host memory, and a card A/B. Worth ~0.5 ms of
a 4.8 ms MoE token (~+10%) and the same absolute on the dense (~+3%) if the pickup reading is right.

## 16:18 (box clock): the native MoE reference on the 3090 - `logs/native-3090/moe-*`

`native-ref.sh` on the box: llama-simple 96 greedy tokens under `nsys profile --cuda-graph-trace=node`, graphs on
and off, then the same without the profiler and llama-bench tg128. The 3090 (936 GB/s, 2,115 MHz) is not the 5090,
so kernel times scale, but the structure carries. **tg128 native on the 3090: 174.3 tok/s with graphs, 134.6 without
(+29% from graphs).** Analyzer (`native-gaps.py`, per token after the first 4, graphs on): 1,291 kernels; pure
execution 4,443 us; **gaps inside the token 181 us in total, 0.14 us a gap**; kernel span 4,625 us. Graphs off:
1,346 kernels, execution 4,755 us, **gaps 7,214 us, 5.36 us a launch** (under the profiler) - native without graphs
is host-bound the way the shim was this morning. Per kernel, graphs on: mul_mat_vec_q 271 a token at 8.82 us (ours on
the 5090: 312 at 6.6 us including the gap; the bandwidth ratio is 1.9x), quantize_q8_1 1.33 us (ours 1.65 with the
gap), rms_norm 2.19 (ours 2.04). Boundary: the profiler inflates it (5.1 ms in the trace vs a plain llama-simple
period of 8.4 ms and a bench period of 5.74 ms); from the bench period minus the kernel span, **native's per-token
boundary in the bench loop is ~1.1 ms**, about ours (~0.8-1.0 ms). llama-simple adds ~2.6 ms a token of its own
(sampling on that CPU) natively, ~1.3 ms on the Mac - the bench is the vehicle for boundary comparisons. The two
native greedy decodes differ from each other (graphs on vs off, and even the same configuration twice): native
llama.cpp on this build is not run-to-run deterministic; the shim's is.

**Reading for the MoE gap (216 vs 245 native on the 5090, ~12%):** the boundary is at parity; what native has
that we do not is a 0.14 us gap between kernels inside a token against our ~0.3-1.3 us dispatch gap (in-chain mean
interval 2.45 us) and our chain seams (~0.4 ms a window). Those are the driver's own per-launch path and the seams -
a host-side graph replay would take the seams to ~0.1 ms (host ~1.7 us a launch under the engine's 2.45) for ~+6%;
the keep-alive kernel targets the engine's pickup at the boundary (~0.3-0.5 ms) for a similar amount.

## 15:40: the keep-alive kernel (`0d6d629`, `TINYNV_KEEPALIVE=1`)

Built as planned: after every synchronisation the driver launches one warp (libtinycudart/keepalive.cu, sm_120a from
the 3090 box's nvcc) that polls a flag in a host page and exits when the host sets it - just before the next caller
chain is handed over, or before any wait - or after `TINYNV_KEEPALIVE_US` (2,000) as the watchdog. Off unless asked.
**Prediction:** text byte-identical; the kernel profile's boundary bucket (which now lands on the keep-alive launch,
the first launch after the wait) shrinks and the first real kernel of the token follows the release within tens of
us; MoE tg128 +5-10% interleaved (0.3-0.5 ms of a 4.6 ms token), dense +2-3%. Refusal readings: no change means the
pickup latency is not idle-related (the engine was awake and still took ~0.5 ms); a wait that sits out 2 ms per token
means the release did not reach the kernel (the counters say which path released).

## 16:30 (box): the native dense reference on the 3090 - `logs/native-3090/dense-*`

tg128 native on the 3090: 42.9 tok/s with graphs, 40.4 without (+6%: the dense is kernel-bound there too). Per
token (graphs on): 1,669 kernels, pure execution 18,927 us, gaps 94 us (0.06 us a gap), i.e. 15.3 GiB of weights at
~810 GB/s = 87% of the 3090's 936 GB/s. Ours on the 5090: 12.0 ms in kernels = 1.27 TB/s = 71% of 1,792; native on
the Windows 5090 is 13.3 ms a token in total, so its kernels run at roughly the same fraction - the 5090 reaches
~70% with these kernels, and our dense kernel time is at parity with native. What is left on the dense is the
boundary, as on the MoE.

## 15:25-15:36: the keep-alive measured - no change, and what the boundary is made of

Three attempts before it ran as designed: (1) launched from the stream sync, which a decode never reaches (the logits
download waits for itself inside the driver and the sync after it is answered without a wait) - zero launches;
(2) launched after every download and every real sync - 2,287 launches for 96 tokens, each released by the next of
the ~24 small read-backs a token, **-20%**; (3) one launch a token but every wait released it - no effect. Now
(`launches_since_ka >= 64`, waits ignore a pending keep-alive while it is the last thing on the compute queue, its
submit does not mark the stream as needing a wait, fini lets it go and waits): **96 launches, 94 released by the
next chain's hand-over, text identical, and tg128 211.5 / 215.9 default vs 209.5 / 213.8 with it - no change.** The
profile's boundary bucket fell 996 -> 872 us and the kernel bucket rose by the same: the engine was never asleep.

So the boundary (~0.9-1.0 ms a window) is made of the logits copy (~150 us), the host's turnaround (161 us), the
first chain's build (128 x 2.2 us = ~280 us, during which the engine has nothing) and the first kernel plus
submission latency - and the short first chain could not hide the build because the host builds a chain no faster
than the engine runs one (2.2 vs 2.45 us a launch), so the engine simply waits at the next seam instead. **The one
lever left for both models is the driver's own cost a launch (1.74 us: 1.29 building the descriptor, 0.45 in the
API layer).** Cutting it to ~1 us would shorten the first chain's build by ~150 us, close most of the ~0.4 ms of
seams a window, and make a short first chain worth something - ~+10% on the MoE, a few percent on the dense.
`TINYNV_KEEPALIVE` stays a measured knob, off.

## 15:49-15:56: a fresh batch across every model tested so far (`docs/bench/mac-batch-20260919.md`)

Antonio: "let's run a fresh set of benchmarks on all the models we've tested so far ... give me a webpage with the
results, and a separate webpage with progress made and how." Binaries relinked clean at `b98b01b`; the four models
no longer in this checkout (Llama 3.1 8B Q8_0, Nemotron Nano 9B Q6_K, Gemma 4 12B Q4_K_M, Gemma 4 26B-A4B Q4_K_XL)
were read in place from the shared tree. Every step clean. Decode vs native: 97% / 78% / 98% / 98% / 98% / 88%
(8B, Nemotron, Gemma 12B, Gemma 26B-A4B, 27B, Qwen MoE); prefill above native on most. MTP 101 tok/s greedy at 37%
acceptance on the Thunderbolt prompt; the three images byte-identical, Z-Image's total 10.22 s once its encoder and
VAE were warm again (the batch's 16.6 s was a cold load after the day's 36 GB of model copies; the sd step pre-reads
only the main model file - worth extending to the --vae/--llm files). Nemotron Nano at 78% is the one model clearly
behind: its state-space kernels have not been profiled per kernel. Two pages published from this: "macuda
Benchmarks" (the results) and "macuda Progress" (the week's numbers and how they moved). First scripting attempt
used a shell variable named `path` inside zsh, which is the PATH array - nothing reached the card; noted here so it
is not relearned.

## 16:05: the last 12% - the submit's cost split, and a token's batches counted

Antonio: "let's work on that last 12%". The launch profile now times a submit's two halves and keeps an untrimmed
history of the last 128 submissions. MoE decode, host quieter (182 tok/s in `simple`): inside `tinynv_launch` 1.45
us a launch = 0.52 building the launch + 0.49 flush share + 0.44 API layer; of the flush share, **announcing (the
fence read's round trip to the TinyGPU server, then the doorbells) is 0.50 us a launch = ~25 us per announcement**,
the posted writes 0.04. A token is 13 compute chains and one copy batch: the first chain 2,289 dwords (the held
input uploads plus its patches), every other chain **29 dwords** - the delta delivery sends essentially nothing
mid-token - and the logits download 33. The 1,740 copy-engine batches of the run were the load and the downloads.
So a token pays 14 announcements (~0.35 ms of host time) and 13 chain seams (~40 us each on the engine, ~0.5 ms).
**Prediction:** deeper chains after a short first one cut both: `TINYNV_CHAIN_DEPTH=512 TINYNV_SHORT_FIRST_CHAIN=128`
takes a token from 14 to ~5 announcements and ~4 seams, +5-8% MoE interleaved; depth 256 with the same first chain
about half that; the morning's "1024 is 14% slower" was taken with the host at -O0, when the first chain's build
dominated. Refusal reading: no change means the seam is not per-batch latency but the engine draining, and the lever
is the host's lead (the pipelined fence read).
**Measured 16:11-16:19.** Deeper chains after a short first one: default 215.0/214.5 vs depth 512 + first 128
**205.8/205.9 (-4%)** vs depth 256 + first 128 218.6/213.3 (a wash): a chain handed over late idles the engine
longer than the seams it saves, because the host builds a chain no faster than the engine runs one (1.86 vs 2.45 us
a launch). Then the fence read pipelined (`TINYNV_RING_ASYNC=1`: the read is sent with the batch, the reply taken and
the doorbell rung sixteen launches into the next chain; the transport gained a split read that drains itself before
any other request, and the launch entry no longer takes the lock twice per launch): text identical, the fence
verified reaching the device, announcing 0.50 -> 0.19 us a launch but a launch 1.01 -> 0.95 only (the reply takes
longer than sixteen launches to come back: the read is a non-posted PCIe read across the Thunderbolt tunnel, not
only a socket hop), and **tg128 unchanged on both models** (MoE 215.1/213.6 vs 211.3/215.1; dense 73.6/74.0 vs
74.0/73.7). Both stay knobs, off.

**So the host-side levers are measured out, and the budget says where the 12% is.** MoE bench token 4.65 ms:
~3.45 ms of kernels at 2.45 us a launch including the dispatch gap (native's gap inside a graph is 0.14 us: ~0.4
ms a token of ours is the engine fetching and dispatching 1,400 descriptors one by one), ~0.4 ms of chain seams
(13 batches a token, each a pickup), and a ~0.7 ms boundary that is at parity with native's bench loop. Native's
4.1 ms token has one submission and no seams. The delta path's own accounting is the key fact: **every chain but
the first carries 29 dwords of patches a token - a decode's descriptors are byte-identical token to token.** That
is what makes a driver-level replay cheap now: keep a token's descriptors resident, link all of its chains into one
(no build-ahead problem, because nothing is built), patch the timeline values and the first chain's inputs, submit
once. Seams go to zero, the first chain's build leaves the boundary, the host stops mattering: an estimated 4.65 ->
~4.0 ms a token (~+15%, parity). The capture boundary is ggml's own (the CUDA-graph subset in the runtime layer,
plan doc option B'), which needs the ggml-cuda host halves rebuilt with `-DGGML_CUDA_USE_GRAPHS` - a host-only
rebuild, now routine. This is the work for the last 12%.

## 16:25-16:50: the resident-chain replay built (`TINYNV_GRAPH_RESIDENT`) - the CUDA-graph subset first

Step one (`12734b3`): the runtime layer's CUDA-graph subset; ggml-cuda's host halves rebuilt with
`-DGGML_CUDA_USE_GRAPHS` (`libggml-cuda.nographs.a` kept). ggml captured the MoE decode once (1,634 nodes) and
replayed it 94 times, text byte-identical; interleaved tg128 graphs off vs on: **MoE 211.6/214.4 -> 220.8/220.6**,
dense 73.7/73.9 -> 74.2/73.9; the caller's time between launches 0.41 -> 0.09 us. Step two: the driver records a
replayed token into a region of its own as one linked chain (the descriptor build factored into `build_qmd`, shared),
seals it with one copy-engine transfer, and every replay is one batch: the tail's release payload patched inline,
an acquire on the previous work, the launch of the head. Off unless `TINYNV_GRAPH_RESIDENT=1`; a recording the
driver declines (null device, wrong chain shape, copies after launches) falls back to launch-by-launch replay.
**Prediction:** text byte-identical; the counters say one recording and ~94 one-batch replays; MoE tg128 from ~220
to 235-250 (native 245), dense from 74 to ~76 (native 75.3). Refusal readings: a wait timeout or fault on the first
replay means the linked chain or the inline tail patch is wrong; unchanged tg128 means the seams were not the cost.

## 17:18-17:28: the graph gate caught a wrong image - CUDA graphs are NOT the default

The graphs-on build passed everything llama.cpp has: op-verify 450/450 at three depths, both greedy texts
byte-identical, ggml capturing once and replaying 94 times, MoE tg128 211.6/214.4 -> 220.8/220.6. Then the image
models: **SD 1.5 wrote a 9,309-byte PNG where the reference is 574,050 - a blank image - and its sampling time
halved (3.35 s -> 1.87 s).** Confirmed on the same binary with `GGML_CUDA_DISABLE_GRAPHS=1`: graphs off reproduces
the reference byte-identically, graphs on does not. The halved time says the replay skipped real work.

**Cause, stated as a hypothesis and not yet verified:** a recorded node keeps the kernel parameters as they were
marshalled at capture, and stable-diffusion.cpp's per-step scalars (the timestep and sigma) travel in those bytes
while the tensors they sit beside keep the same pointers and shapes - so ggml sees node properties unchanged, never
re-captures, and every step after the first replays the first step's scalars. NVIDIA's graphs survive this only
because ggml updates kernel-node parameters on the instance between launches, which this subset does not implement.
llama.cpp's decode is unaffected because what changes per token is memory contents, not marshalled scalars.

**So the shipped archive went back to the no-graphs build and every binary was relinked** (`libggml-cuda.a` =
the no-graphs build; `libggml-cuda.graphs.a` keeps the graphs-on one for llama-only experiments). Re-gated at
`c179183` on the restored default: op-verify 450/450 x3, both texts identical, **all three images byte-identical**
to the 15:5x batch (SDXL 2.76 s, SD 1.5 3.91/3.35, Z-Image 10.41/8.60), server + MTP soak 230 requests, 0 errors,
58 probes, 0 mismatches, 124.3 tok/s. Card idle warm, lock free, preflight OK.

**Where the last 12% stands.** Three things were built and measured today and none is a default: the pipelined
announcement (no change), the resident-chain replay (`TINYNV_GRAPH_RESIDENT`, at parity: the engine's own clock
puts a resident 128-chain at ~285 us including hand-over and a token at 4.39 ms against 4.82 for the per-node
replay, but llama-bench reads 224.7 vs 223.3), and the CUDA-graph subset itself (a real +4% on the MoE for
llama.cpp, wrong output for stable-diffusion.cpp). **The graph subset is worth finishing** - it needs per-node
parameter updates so a replay re-reads the caller's current scalars, which is what makes it safe for sd and is
also what ggml's own update path expects. That is the next piece of work on this thread, and it is the one that
unlocks both the +4% and the resident chain underneath it.

## 17:40-18:04: the blank image was two holes in the capture, both mine - graphs now gate clean and are the default

Rather than build the parameter update my hypothesis called for, the capture was made to account for itself: every
call that happened while a capture was active and was not recorded now reports itself by name at exit, and so does
any work issued on a stream other than the captured one. One `sd` run named the first hole - **16 calls to
`cudaMemcpy2DAsync`** (the strided copy) ran during the capture and were never replayed. Recording it as a node kind
raised the node count by exactly 16 and left the image still blank, which killed the staleness hypothesis outright:
a one-step render was wrong too, so the very first replay was already missing work rather than going stale.

The capture summary then named the stages (269 launches with `k_get_rows_float` first = the text encoder; 1,158 with
the copies = the diffusion model) and the second hole followed from what those stages are made of: **libtinycublas
called `tinynv_launch` directly**, so every unquantized GEMM - most of sd's arithmetic - bypassed the capture
entirely, ran once while recording and never on a replay. Routing the four cuBLAS launch sites through the same
capture check fixed it: **SD 1.5 with graphs on is byte-identical to the reference at 3.35 s sampling**, its own
correct time.

**Gated at graphs-on and adopted as the default** (`libggml-cuda.a` is now the `-DGGML_CUDA_USE_GRAPHS` build,
`libggml-cuda.nographs.a` keeps the other): op-verify 450/450 at 32/64/128; both greedy texts byte-identical; all
three image models byte-identical (SDXL 2.79 s, SD 1.5 3.95/3.39, Z-Image 10.07/8.43); server + MTP soak 233
requests, 0 errors, 59 probes, 0 mismatches, 124.8 tok/s. Interleaved tg128, same driver, graphs off vs on:
**MoE 211.4/216.7 -> 223.5/221.7 (+4%)**, dense 73.9/73.8 -> 74.1/73.8 (parity). MoE is now ~91% of native (245).

**The lesson worth keeping:** both bugs were work that left through a door the capture did not watch, and neither
was visible as an error - only as a wrong picture. An instrument that counts what it could not record found both in
two runs, after a plausible-sounding hypothesis about stale kernel parameters had sent me the wrong way. The
accounting stays in the tree for the same reason.

## 20:30: Nemotron Nano 9B at 78% of native - the last clearly-behind model, never profiled

Antonio gave the card back with "run through them as you feel". Card re-enumerated while it was on the PC; cleared
per the standing authorisation (link up 16 GT/s x4, DART clean, only the dext instance newer), preflight OK.

Nemotron Nano 9B v2 Q6_K reads 109.8 tok/s against native's 140.6 - 78%, where every other model is 92-101%. It is
a Mamba-hybrid (`nemotron_h`) and its state-space path has never been through `TINYNV_KERNEL_PROFILE`.
**Prediction on record:** the per-token kernel list is dominated by the state-space kernels (`ssm_conv_f32`,
`ssm_scan`, `gated_delta_net_cuda`) rather than by matmuls, and the gap is one of two shapes - either those kernels
are slow in absolute terms (execution-bound, so the answer is upstream's kernels and not this driver), or the token
carries far more launches than its size suggests and it is launch-bound the way the MoE was this morning. The
discriminator is the mean in-chain interval: ~2.5 us like the MoE means launch-bound, much larger means the kernels
are the cost. Refusal reading: if the list looks like an ordinary dense model's, the 78% is not in the SSM path at
all and I am looking in the wrong place.

**Measured 20:53-21:07, and the prediction was wrong in a useful way.** The per-kernel profile said Nemotron is
NOT launch-bound the way the MoE was, and not state-space-kernel-bound either: 598.8 launches a window but 10,043 us
in kernels, a mean interval of 16.8 us against the MoE's 2.45. The kernels are big. What the profile then showed was
the real thing: **73.9 chain-first intervals a window totalling 3,679 us** - the chain was averaging 8 launches, not
128. The capture explains it: Nemotron's token graph holds **611 launches and 81 device-to-device copies interleaved
among them**, and every copy on the copy queue makes `batch_begin` hand the pending chain over first. 81 copies, ~74
seams, 3.7 ms of a 10 ms token.

**Fix (`abd0458`, now the default):** serve a device-to-device copy with the copy kernel the runtime layer already
lends, as an ordinary caller launch. It joins the chain - ordering is unchanged because the chain provides it, and
no queue switch happens. `TINYNV_DTOD_VIA_COMPUTE=0` puts them back on the copy engine. Launch chains over a run
fell 7,360 -> 594, copy-engine batches 8,618 -> 897. **Interleaved tg128: Nemotron 111.3/109.7 -> 143.5/144.0
(+30%), prefill 6,422/6,437 -> 7,128/7,191 (+11%); Qwen MoE 219.5/222.9 -> 222.0/223.4 and dense 27B 74.1/74.1 ->
74.2/73.9, both unchanged** - the shape a targeted fix should have. **Nemotron is now ~102% of its native 140.6**,
from 78% this morning, and the last clearly-behind row in the table is gone. Gated: op-verify 450/450 at three
depths, both greedy texts byte-identical, all three images byte-identical, MTP soak 231 requests / 0 errors /
0 mismatches / 124.3 tok/s.

**Worth keeping:** the prediction named the state-space kernels and the launch count, and both were wrong. The
instrument found it anyway, because it reports the chain seams separately from the kernels - a number nobody was
looking at for this model. Third time today that measuring beat reasoning.
